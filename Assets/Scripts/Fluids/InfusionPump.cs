//generic infusion pump class
//to handle toggling model and visibility as well as passing commands

using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InfusionPump : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
